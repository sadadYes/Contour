# Contour

Contour is a simple game developed in C using the raylib library. The game features a player character that can move around a grid, attack enemies, and has a health system. Enemies spawn randomly around the player and move towards the player.

## Getting Started

These instructions will get you a copy of the project up and running on your local machine for development and testing purposes.

### Prerequisites

- GCC Compiler
- raylib library

### Building

The project uses a Makefile for building. To build the project, navigate to the project directory and run the following command:

```sh
make